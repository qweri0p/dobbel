{
  "name": "dobbel",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist",
    "upload": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "github:tschaub/gh-pages#dependabot/npm_and_yarn/async-3.2.2",
    "vite": "^2.9.13"
  },
  "dependencies": {
    "jquery": "^3.6.0"
  }
}
